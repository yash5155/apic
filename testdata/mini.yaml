openapi: 3.0.0
info:
  title: Yaml Service
  version: "2.0"
servers:
  - url: https://yaml.example.test
paths:
  /health:
    get:
      summary: Health check
      parameters:
        - name: verbose
          in: query
          schema:
            type: boolean
